% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdmx_output.R
\name{as_sdmx_compatible}
\alias{as_sdmx_compatible}
\title{Add an SDMX-compatible observation profile to MICS cell results}
\usage{
as_sdmx_compatible(cells, config, table_id = NULL, table_context = NULL)
}
\arguments{
\item{cells}{A data frame with one row per table cell.}

\item{config}{A list containing \code{frequency}, \code{ref_area}, \code{survey_id}, and
\code{time_period}.}

\item{table_id}{Table identifier. Required for raw engine cell results.}

\item{table_context}{Optional parsed tabulation context used to recover
multi-level column headers and suppression settings.}
}
\value{
A tibble with one row per observation and the 35 standardized fields.
}
\description{
Converts either the package's engine cell results or an already standardized
MICS cell table into a 35-variable long-format contract adapted from the
mics-translation project. The output omits the custom \code{table_id}, \code{value},
\code{estimate}, \code{digits}, and \code{unw} duplicates; use \code{TABLE_ID}, \code{OBS_VALUE},
\code{DECIMALS}, and \code{denominator_n} instead. Input fields are unchanged.
Source coordinates remain in \code{row_order} and \code{column_order}; the redundant
\code{row_id}, \code{column_id}, \code{ROW_ID}, and \code{COLUMN_ID} fields are omitted.
The output also omits \code{unweighted_n}, which duplicates \code{denominator_n} for
engine cells. For standardized input, the supplied \code{denominator_n} is retained.
The optional input \code{unit_measure} override is applied to \code{UNIT_MEASURE} but
is not included as a separate output field.
}
