% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_files.R
\name{download_survey_files}
\alias{download_survey_files}
\title{Download survey tabulation plans and preparation files}
\usage{
download_survey_files(
  surveys = NULL,
  files = NULL,
  dest_dir = "inputs",
  overwrite = FALSE,
  quiet = FALSE,
  repo = "bariscr/micsPlusTableR-Files",
  ref = "main"
)
}
\arguments{
\item{surveys}{Character vector of root survey folder names, such as
\code{"JAM_W1"}. \code{NULL} selects all surveys.}

\item{files}{Character vector of exact repository-relative paths from
\code{\link[=list_survey_files]{list_survey_files()}}. \code{NULL} selects all files in the selected surveys.}

\item{dest_dir}{Destination folder, relative to the current R working
directory unless an absolute path is supplied. Defaults to \code{"inputs"}.}

\item{overwrite}{Replace existing files? Defaults to \code{FALSE}; any existing
selected file stops the operation before downloading file contents.}

\item{quiet}{Suppress file download progress and the completion message?}

\item{repo}{GitHub repository in \code{owner/name} form.}

\item{ref}{Branch, tag, or commit to download. Defaults to \code{"main"}.}
}
\value{
Invisibly returns the selected file-list rows with \code{survey}, \code{path},
\code{size}, \code{url}, and \code{local_path} columns, plus the resolved \code{commit} attribute.
}
\description{
Save selected plans, preparation scripts, and supporting reference assets
from the public survey files repository. Household and household-member
microdata must be obtained separately with the required permission.
}
\details{
Survey folders use a three-letter country code and wave, such as \code{JAM_W1}.
Downloads include root-level Excel plans (\code{.xls}, \code{.xlsx}) and \code{.R} scripts
or \code{.RData} reference assets inside \verb{prep-files-*} subfolders. Other files,
including SPSS microdata and hidden files, are excluded. Preparation
subfolders are preserved. A single Git commit is resolved before listing or downloading,
so changes to the branch during a download cannot mix remote versions.
Newly published survey folders are discovered without a package update.

Downloads are staged and their sizes checked before destination files are
written. A network failure leaves existing destination files unchanged.
Files are downloaded in binary mode and are never executed. Selecting
individual files may omit preparation helpers; select a whole survey folder
for all its published materials. Microdata must still be obtained separately.
Git LFS pointers are not supported.

Internet access is required. Unauthenticated GitHub API rate limits apply;
if a request fails, check the connection and public repository/ref, or retry
later. The browser ZIP link in the user's guide is an alternative.
}
\examples{
\dontrun{
download_survey_files("JAM_W1")
download_survey_files(c("JAM_W1", "MNG_W2"), dest_dir = "survey-inputs")
download_survey_files() # All surveys
available <- list_survey_files()
download_survey_files(files = available$path[1])
}
}
\seealso{
\code{\link[=list_survey_files]{list_survey_files()}}
}
