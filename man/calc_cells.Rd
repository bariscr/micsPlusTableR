% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_helpers.R
\name{calc_cells}
\alias{calc_cells}
\title{Calculate individual horizontal table cells}
\usage{
calc_cells(df, tab_r, tab_c3, tab, weight_var, weighted = FALSE)
}
\arguments{
\item{df}{Prepared data frame, after block filters and calculations.}

\item{tab_r}{Row specification with \code{row_index} and \code{row_lgc}.}

\item{tab_c3}{Column specification with \code{col_index}, \code{col_condition}, and
\code{col_var_name}; optional \code{col_logic} preserves display labels.}

\item{tab}{Cell specification with \code{row_index}, \code{col_index}, and \code{stat_type}.}

\item{weight_var}{Weight-column name. Unweighted calculations may use NA.
The \code{p_sum} statistic always needs a weight column.}

\item{weighted}{FALSE for unweighted results, TRUE for weighted results,
or \code{"both"} (legacy mode, returns the unweighted result only).}
}
\value{
A tibble with row/column indices, logic, statistic, and numeric value.
}
\description{
Calculate individual horizontal table cells
}
\details{
This background helper is available for explicit cell tests and
code-only work; app users normally use Tabulator. Supported statistics
include counts, indicator percentages, numeric means, medians, and
constant totals. See \link{statistic-types} for the complete horizontal forms
and their populations. Row mutations in \code{row_lgc} execute in Excel row
order before row predicates and remain available to subsequent rows.
With \code{n_unw} or \code{n_unw1}, the column condition \code{hhmembers} instead sums
\code{HLnum} for row-selected records with \code{total == 1}. This is a household
member base from household records, not an ordinary record count.
Worksheet aliases such as \code{total1 == 1} and \code{totalHL == 1} are converted
by \code{\link[=read_mics_tabulation]{read_mics_tabulation()}}, not this helper; see \link{worksheet-conditions}.

Bare \code{mean} is an indicator percentage; \code{mean(variable)} averages the named
variable. Median and \code{mean_unw} forms are unweighted, as are legacy
capitalized \code{Mean} indicators. The \code{p_sum} statistic divides the variable
sum by the weight sum within both conditions, then multiplies by 100.

Missing statistic cells return NA. An empty denominator can produce NaN.
Optional \verb{d=} arguments are returned as \code{display_digits} metadata and never
round the calculated value; see \link{statistic-precision}.
}
\examples{
calc_cells(data.frame(total = c(1, 1)),
  data.frame(row_index = 9L, row_lgc = "TRUE"),
  data.frame(col_index = 3L, col_condition = "total == 1", col_var_name = "total_1"),
  data.frame(row_index = 9L, col_index = 3L, stat_type = "n_unw"),
  weight_var = NA_character_)

# Weighted count of sex == 2: 5. Use weighted = FALSE for a count of 2.
calc_cells(
  df = data.frame(total = 1, sex = c(1, 2, 2), weight = c(1, 2, 3)),
  tab_r = data.frame(row_index = 9L, row_lgc = "TRUE"),
  tab_c3 = data.frame(col_index = 3L, col_condition = "sex == 2",
                     col_var_name = "sex_2"),
  tab = data.frame(row_index = 9L, col_index = 3L, stat_type = "n"),
  weight_var = "weight", weighted = TRUE
)
}
