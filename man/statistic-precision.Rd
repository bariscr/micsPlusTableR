% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistic_precision.R
\name{statistic-precision}
\alias{statistic-precision}
\title{Worksheet display precision}
\description{
Every supported worksheet statistic accepts an optional named \code{d} argument
specifying the number of decimal places to display.
}
\details{
Use \code{mean(HCS8, d=0)} for a whole-number expense mean,
\code{mean(d=1)} for a mean indicator with one decimal place, \code{p(d=2)} for two
percentage decimals, or \code{n(d=1)} for a count with one displayed decimal.
Existing argument forms retain their arguments: \code{median(age, d=2)},
\code{mean_unw(age, d=0)}, \code{p_sum(x, d=2)}, and \code{p(100, d=2)}.
A constant may use \code{100(d=2)}. The statistic must be supported in the table's
direction; adding \code{d} does not change the calculation or its weighting.

\code{d} must be a literal non-negative whole number, supplied once. Zero means
no decimal point. Omit \code{d} to keep the existing formatting for that statistic
and output mode; means still default to one decimal place. Spaces around
arguments are allowed. \code{d} means decimal places, not significant figures.

The Formatted and Suppressed Tabulator views honor explicit precision for
visible numbers; Unformatted retains full numeric estimates. Excel applies
number formats in both output workbooks, including multi-sheet runs.
Parentheses retain the requested precision; \verb{(*)} and \code{-} remain markers.
Display rounding does not alter stored estimates, calculations, weights,
denominators, suppression thresholds, or consistency checks.

Internally, \code{stat_type} contains the statistic without \code{d}; \code{display_digits}
holds the explicit integer or NA when omitted. Worksheet filling carries the
statistic and its precision together. Changing only \code{d} does not change mean
filter boundaries or the identification of \code{n_unw} before IDX.
Long-format output uses the override in \code{DECIMALS} and display strings while
retaining full precision in \code{OBS_VALUE}.
}
\seealso{
\code{\link[=tabulate_h]{tabulate_h()}}, \code{\link[=tabulate_v]{tabulate_v()}}, \code{\link[=write_mics_table]{write_mics_table()}}
}
