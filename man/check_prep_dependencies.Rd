% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_dependencies.R
\name{check_prep_dependencies}
\alias{check_prep_dependencies}
\title{Check dependencies of a preparation script and its helpers}
\usage{
check_prep_dependencies(prep_script, stop_on_missing = FALSE)
}
\arguments{
\item{prep_script}{Path to a preparation R script.}

\item{stop_on_missing}{Stop with an installation command if a required
package is missing or cannot be loaded. Defaults to FALSE for inspection.}
}
\value{
A data frame with \code{package}, \code{required_by} (script basenames),
\code{available}, and \code{problem} (NA when loadable).
}
\description{
Inspect preparation code without executing it, then check whether each
required package can be loaded. App dependencies and survey-specific
preparation dependencies install together and are documented separately in
\link{prep-script-dependencies}. This is a diagnostic tool; preparation checks
dependencies automatically.
}
\details{
Reads the script's literal \code{prep_dependencies} declaration,
qualified calls, library/require calls with literal package names, and
known legacy survey/FIES function calls. It also inspects all R files in
the sibling \code{FIES-inputs} folder and recursively follows literal source
paths. Dynamically computed package names and source paths require an
explicit \code{prep_dependencies} declaration in the main script.
This checks package availability, not numerical correctness or version
compatibility of the survey code. It never executes preparation code.
}
\seealso{
\code{\link[=install_prep_dependencies]{install_prep_dependencies()}}, \code{\link[=prepare_mics_data]{prepare_mics_data()}}
}
